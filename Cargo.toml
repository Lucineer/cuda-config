[package]
name = "cuda-config"
version = "0.1.0"
edition = "2021"
description = "Configuration management — layered configs, environment variables, defaults, validation, hot reload, and schema enforcement"

[dependencies]
serde = { version = "1", features = ["derive"] }
